# VCS and editor state
.git
.DS_Store

# Theos / local build artifacts
.theos/
packages/
obj/
*.deb
relay/rctl-relay

# Frontend local artifacts. Docker builds the control web client itself.
web/node_modules/
web/dist/
web/*.tsbuildinfo
relay/web-admin/node_modules/

# Debug captures may include private device screen/audio data.
*.png
*.h264
*.bin
.playwright-mcp/

# Secrets and per-user relay artifacts must never enter the Docker build context.
.env
.env.*
!.env.example
relay.env
relay/.env
relay/data/
*.secret
*.token
*.pem
*_ed25519
*_ed25519.pub
*.p12
*.mobileprovision
personalized/
relay-config.plist
