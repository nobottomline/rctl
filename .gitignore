# Theos / build artifacts
.theos/
*.deb
packages/
obj/
.DS_Store

# Debug captures (may contain the device's screen — never commit)
*.png
*.h264
*.bin
.playwright-mcp/

# Secrets / keys — never commit
*.pem
*_ed25519
*_ed25519.pub
*.p12
*.mobileprovision
