# Legacy Control Client

This directory preserves the former single-file control implementation for
reference and regression archaeology. It is not shipped as the current client.

Implement fixes and features in the parent `web/` application unless a task
explicitly concerns historical behavior.
